import { Text, View } from "react-native"

const Home = () => {
    return (
        <View className="flex-1 items-center justify-center bg-green-100">
            <Text className="text-lg font-semibold text-green-800">Home Screen</Text>
        </View>
    )
}

export default Home