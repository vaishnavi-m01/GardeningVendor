import { Text, View } from "react-native"

const Profile = () => {
    return (
        <View className="flex-1 items-center justify-center bg-blue-100">
            <Text className="text-lg font-semibold text-blue-800">Profile Screen</Text>
        </View>
    )
}

export default Profile